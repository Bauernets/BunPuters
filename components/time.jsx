import React from "react";

export default function Time(props) {
    return (
        <div>
            <h1>Hello, world!</h1>
            <p>Current time is: {props.time}</p>
        </div>
    );
}